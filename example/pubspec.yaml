name: audioplayer_example
description: a audio player plugin example
version: 2.1.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  path_provider:
  http:
  audioplayer_ns:
    path: ../audioplayer_ns
  audioplayer_web_ns:
    path: ../audioplayer_web_ns

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
